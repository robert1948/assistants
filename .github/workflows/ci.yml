name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  repository-checks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate required files
        run: |
          test -f README.md
          test -f LICENSE
          test -d src
          test -d tests
          test -d docs

      - name: Confirm clean line endings in markdown
        run: |
          if grep -IRn $'\r' -- *.md docs/ src/ tests/; then
            echo "CRLF line endings found. Please use LF."
            exit 1
          fi
